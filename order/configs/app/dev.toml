[app]
mode='development'

[api]
host = "localhost"
port = 8000
base_url_prefix = "/api/v1"

[db]
host = "localhost"
port = 5432
database = "orders"
user = "postgres"
password = "postgres"
migration = true
logging = true
max_idle_connection = 100

[broker]
host = "localhost"
port = 5672
login = "admin"
password = "admin"
max_channels = 10

[cron]
seconds = 5

[logging]
log_output=''
log_level="info"

[cache]
host="localhost"
port=6379
password=""
db=0